{
  "name": "shopify-gacha-app",
  "version": "1.0.0",
  "private": true,
  "description": "Shopify向け有料ガチャ機能。抽選はサーバー(Vercel Functions)で行い、確率・価格・景品はSupabaseの値として管理する。",
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/nodemailer": "^8.0.1",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.4"
  }
}
